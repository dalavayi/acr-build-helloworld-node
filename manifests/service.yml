apiVersion: v1
kind: Service
metadata:
    name: dalavayiacrbuildhelloworldnode
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: dalavayiacrbuildhelloworldnode